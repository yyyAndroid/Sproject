<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.abe.dwwd.sporjectone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 视频录制的权限star -->
    <!-- 摄像头 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 音频即声音 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- sd卡写入权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 硬件支持 -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <!-- 视频录制的权限end -->
    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.ChartActivity" />
        <activity android:name=".ui.activity.OkHttpActivity" />
        <activity android:name=".ui.activity.ViewPageActivity" />
        <activity android:name=".ui.activity.MoveActivity" />
        <activity android:name=".ui.activity.PullToRefreshActivity" />
        <activity android:name=".rxjava.RxJavaActivity" />
        <activity android:name=".rxjava.RxJavaSecondActivity" />
        <activity android:name=".rxjava.RxJavaVaryActivity" />
        <activity android:name=".ui.activity.SweetAlertDialogActivity" />
        <activity android:name=".ui.activity.VLayoutActivity" />
        <activity android:name=".ui.activity.SearchViewActivity" />
        <activity android:name=".ui.activity.PathPainterActivity"></activity>
    </application>

</manifest>